diff --git a/src/pages/EditArticlePage.tsx b/src/pages/EditArticlePage.tsx
index 1b304f6..29484a3 100644
--- a/src/pages/EditArticlePage.tsx
+++ b/src/pages/EditArticlePage.tsx
@@ -1,4 +1,5 @@
 import useInputs from '@/lib/hooks/useInputs';
+import { useDraft, getDraft } from '@/lib/hooks/useDraft';
 import queryClient from '@/queries/queryClient';
 import { useUpdateArticleMutation } from '@/queries/articles.query';
 import { QUERY_ARTICLE_KEY } from '@/constants/query.constant';
@@ -8,14 +9,33 @@ const EditArticlePage = () => {
   const { state } = useLocation();
   const navigate = useNavigate();
 
-  const [articleData, onChangeArticleData, setArticleData] = useInputs({
+  const DRAFT_KEY = `article-draft-edit-${state.slug}`;
+
+  const initialData = {
     slug: state.slug,
     title: state.title,
     description: state.description,
     body: state.body,
     tag: '',
     tagList: state.tagList,
-  });
+  };
+
+  const savedDraft = getDraft(DRAFT_KEY);
+  const [articleData, onChangeArticleData, setArticleData] = useInputs(
+    savedDraft
+      ? { ...initialData, title: savedDraft.title, description: savedDraft.description, body: savedDraft.body, tagList: savedDraft.tagList }
+      : initialData,
+  );
+
+  const { clear: clearDraftFn } = useDraft(
+    DRAFT_KEY,
+    {
+      title: articleData.title,
+      description: articleData.description,
+      body: articleData.body,
+      tagList: articleData.tagList,
+    },
+  );
 
   const onEnter = (event: React.KeyboardEvent<HTMLInputElement>) => {
     if (event.key === 'Enter') {
@@ -48,6 +68,7 @@ const EditArticlePage = () => {
       {
         onSuccess: (res) => {
           queryClient.invalidateQueries({ queryKey: [QUERY_ARTICLE_KEY] });
+          clearDraftFn();
           const newSlug = res.data.article.slug;
           navigate(`/article/${newSlug}`, { state: newSlug });
         },
@@ -55,6 +76,11 @@ const EditArticlePage = () => {
     );
   };
 
+  const handleClearDraft = () => {
+    clearDraftFn();
+    setArticleData({ ...initialData, tag: '' });
+  };
+
   return (
     <div className="editor-page">
       <div className="container page">
@@ -119,6 +145,16 @@ const EditArticlePage = () => {
                 <button className="btn btn-lg pull-xs-right btn-primary" type="submit">
                   Update Article
                 </button>
+                {savedDraft && (
+                  <button
+                    type="button"
+                    className="btn btn-lg pull-xs-right btn-outline-secondary"
+                    style={{ marginRight: '10px' }}
+                    onClick={handleClearDraft}
+                  >
+                    Clear Draft
+                  </button>
+                )}
               </fieldset>
             </form>
           </div>
diff --git a/src/pages/NewArticlePage.tsx b/src/pages/NewArticlePage.tsx
index 6c458dd..bc3fc0d 100644
--- a/src/pages/NewArticlePage.tsx
+++ b/src/pages/NewArticlePage.tsx
@@ -1,18 +1,39 @@
 import useInputs from '@/lib/hooks/useInputs';
+import { useDraft, getDraft } from '@/lib/hooks/useDraft';
 import queryClient from '@/queries/queryClient';
 import { useCreateArticleMutation } from '@/queries/articles.query';
 import { QUERY_ARTICLES_KEY } from '@/constants/query.constant';
 import { useNavigate } from 'react-router-dom';
 
+const DRAFT_KEY = 'article-draft-new';
+
 const NewArticlePage = () => {
   const navigate = useNavigate();
-  const [articleData, onChangeArticleData, setArticleData] = useInputs({
+
+  const initialData = {
     title: '',
     description: '',
     body: '',
     tag: '',
-    tagList: [],
-  });
+    tagList: [] as string[],
+  };
+
+  const savedDraft = getDraft(DRAFT_KEY);
+  const [articleData, onChangeArticleData, setArticleData] = useInputs(
+    savedDraft
+      ? { ...initialData, title: savedDraft.title, description: savedDraft.description, body: savedDraft.body, tagList: savedDraft.tagList }
+      : initialData,
+  );
+
+  const { clear: clearDraftFn } = useDraft(
+    DRAFT_KEY,
+    {
+      title: articleData.title,
+      description: articleData.description,
+      body: articleData.body,
+      tagList: articleData.tagList,
+    },
+  );
 
   const onEnter = (event: React.KeyboardEvent<HTMLInputElement>) => {
     if (event.key === 'Enter') {
@@ -45,6 +66,7 @@ const NewArticlePage = () => {
       {
         onSuccess: (res) => {
           queryClient.invalidateQueries({ queryKey: [QUERY_ARTICLES_KEY] });
+          clearDraftFn();
           const slug = res.data.article.slug;
           navigate(`/article/${slug}`, { state: slug });
         },
@@ -52,6 +74,11 @@ const NewArticlePage = () => {
     );
   };
 
+  const handleClearDraft = () => {
+    clearDraftFn();
+    setArticleData({ ...initialData, tag: '' });
+  };
+
   return (
     <div className="editor-page">
       <div className="container page">
@@ -116,6 +143,16 @@ const NewArticlePage = () => {
                 <button className="btn btn-lg pull-xs-right btn-primary" type="submit">
                   Publish Article
                 </button>
+                {savedDraft && (
+                  <button
+                    type="button"
+                    className="btn btn-lg pull-xs-right btn-outline-secondary"
+                    style={{ marginRight: '10px' }}
+                    onClick={handleClearDraft}
+                  >
+                    Clear Draft
+                  </button>
+                )}
               </fieldset>
             </form>
           </div>
